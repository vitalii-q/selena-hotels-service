root = "."
tmp_dir = "tmp"

[build]
  cmd = "go build -o ./tmp/hotels-service main.go"
  bin = "./tmp/hotels-service"
  include_ext = ["go"]
  exclude_dir = ["vendor", "tmp"]
  delay = 1000
  stop_on_error = true
